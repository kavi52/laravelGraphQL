type Query {
    # Get all users
    users: [User!]! @all
    # {
    #   users {
    #     id,
    #     name
    #   }
    # }

    # Fetch single user
    user(id: ID @eq): User @find

    #     {
    #   user(id:3){
    #     id,
    #     name
    #   }
    # }
}

type User {
    id: ID
    name: String
    email_verified_at: String
}
